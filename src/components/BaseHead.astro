---
import {
  SITE_NAME,
  SITE_TITLE,
  SITE_DESCRIPTION,
  SITE_LOCALE,
  SITE_LANG,
  ADSENSE_CLIENT_ID,
} from '../consts';
import { withBase } from '../utils/url';
import '../styles/global.css';

// AdSense loader is emitted only in production once a client id is set.
const adsenseEnabled = import.meta.env.PROD && ADSENSE_CLIENT_ID !== '';

interface Props {
  title?: string;
  description?: string;
  /** Path-relative or absolute canonical override; defaults to current URL. */
  canonical?: string;
  /** OpenGraph type, e.g. "website" or "article". */
  ogType?: string;
  image?: string;
  publishDate?: Date;
  modifiedDate?: Date;
  noindex?: boolean;
}

const {
  title,
  description = SITE_DESCRIPTION,
  canonical,
  ogType = 'website',
  image = '/og-default.svg',
  publishDate,
  modifiedDate,
  noindex = false,
} = Astro.props;

const pageTitle = title ? `${title}｜${SITE_NAME}` : SITE_TITLE;
// Astro.url.pathname already includes the base path; explicit `canonical`
// props are root-relative and need the base prefix applied.
const canonicalURL = new URL(
  canonical ? withBase(canonical) : Astro.url.pathname,
  Astro.site
);
const imageURL = new URL(withBase(image), Astro.site);
---

<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<meta name="generator" content={Astro.generator} />

<title>{pageTitle}</title>
<meta name="description" content={description} />
<link rel="canonical" href={canonicalURL} />
{noindex && <meta name="robots" content="noindex, nofollow" />}
{!noindex && <meta name="robots" content="index, follow, max-image-preview:large" />}

<meta name="theme-color" content="#2f6f5e" />

<!-- Google AdSense (only output in production when a client id is configured) -->
{adsenseEnabled && (
  <Fragment>
    <meta name="google-adsense-account" content={ADSENSE_CLIENT_ID} />
    <script
      async
      src={`https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=${ADSENSE_CLIENT_ID}`}
      crossorigin="anonymous"
      is:inline
    ></script>
  </Fragment>
)}
<link rel="icon" type="image/svg+xml" href={withBase('/favicon.svg')} />
<link rel="sitemap" href={withBase('/sitemap-index.xml')} />
<link
  rel="alternate"
  type="application/rss+xml"
  title={SITE_NAME}
  href={new URL(withBase('/rss.xml'), Astro.site)}
/>

<!-- Open Graph -->
<meta property="og:type" content={ogType} />
<meta property="og:site_name" content={SITE_NAME} />
<meta property="og:title" content={pageTitle} />
<meta property="og:description" content={description} />
<meta property="og:url" content={canonicalURL} />
<meta property="og:image" content={imageURL} />
<meta property="og:locale" content={SITE_LOCALE} />
{publishDate && (
  <meta property="article:published_time" content={publishDate.toISOString()} />
)}
{modifiedDate && (
  <meta property="article:modified_time" content={modifiedDate.toISOString()} />
)}

<!-- Twitter -->
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content={pageTitle} />
<meta name="twitter:description" content={description} />
<meta name="twitter:image" content={imageURL} />

<meta http-equiv="content-language" content={SITE_LANG} />
